import { Routes } from '@angular/router';
import { InstallPwaComponent } from './install-pwa/install-pwa.component';

export const routes: Routes = [
  {path:"install-pwa",component : InstallPwaComponent}
];
